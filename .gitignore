# Secrets — never commit.
password.txt

# Build artifacts / tooling state — regenerated by scripts/deploy.mjs.
dist/
.deploy-manifest.json
node_modules/

# Dev-only sources. Only index.html (generated, encrypted) and
# hello_world.html are pushed to GitHub.
dev.html
styles.css
render.js
data.json
draft.html
editor.html
layouts.html
images/
scripts/
.staticrypt.json
.claude/
README.md
